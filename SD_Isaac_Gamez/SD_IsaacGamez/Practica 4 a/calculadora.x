
#include "estructuras.h"


program CALCULADORA{

	version CALC_1{

		float suma(struct operadores) = 1;
		float resta(struct operadores) = 2;
		float multiplicacion(struct operadores) = 3;
		float division(struct operadores) = 4;
	
	}=1;

}=1;